//Write a program that perfoms division of two numbers read from the user and prints out an exact result.  Make sure to test your program with both insteger inputs and decimal inputs

// By setting the varialbes as double the program works with both decimals and integer inputs.

#include <iostream>

using namespace std;

int main()
{
    double x, y, z;

    cout << "Please enter the first number " << endl;

    cin >> x;

    cout << "Plese enter the second number " << endl;

    cin >> y;

    z = x / y;

    cout << "The result is " << z << endl;

    return 0;
}

